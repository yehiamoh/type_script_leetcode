function sortedSquares(nums:number[]) {
 //  return nums.map((e)=>Math.pow(e,2)).sort((a,b)=>a-b); 
   /**
    * 
    * sort function only convert into string and convert it lexographically
    * 
    * To ensure that numbers are sorted in numeric order, you need to provide a comparator function, like (a, b) => a - b. This comparator tells .sort() how to compare two numbers:

   If a - b is negative, a comes before b.
   If a - b is positive, b comes before a.
   If a - b is zero, the two numbers are treated as equal.
    * 
    * 
    */




   let left=0;
   let right=nums.length-1;

   while(left<right){
      
   }

};


